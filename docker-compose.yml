version: '3.8'

services:
  api:
    build: .
    container_name: ovarc-backend
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - NODE_ENV=development